# Dependencies
node_modules/

# Build output
dist/

# Environment secrets — NEVER commit
.env
.env.local
.env.*.local

# OS noise
.DS_Store
Thumbs.db

# Editor noise
.vscode/
.idea/

# Puppeteer Chromium cache (Phase 2+)
.cache/

# Local handoff packages — contain unscrubbed personal data (public repo)
handoff/
output/

# Real-data fixtures and generated PDFs — never commit (public repo)
fixtures/patrick-master-*.json
fixtures/*.pdf
