---
phase: 07-quality-packaging-global-install
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
tags:
  - documentation
  - tracking
  - requirements
must_haves:
  truths:
    - "REQUIREMENTS.md shows QUAL-01 and QUAL-02 as [x] with — closed 2026-07-28 suffix"
    - "REQUIREMENTS.md traceability table shows QUAL-01 and QUAL-02 as Complete — 2026-07-28"
    - "PROJECT.md Active (v1.1) section no longer contains CR-01 or WR-04 entries"
    - "PROJECT.md Validated (v1.1 — in progress) section contains CR-01 and WR-04 as [x] with Phase 7 (2026-07-28) suffix"
    - "PROJECT.md Active (v1.1) section no longer contains the three TYPO Designed PDF entries"
    - "PROJECT.md Validated (v1.1 — in progress) section contains the three TYPO entries as [x] with Phase 05 (2026-07-30) suffix"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal requirement tracking for QUAL-01 and QUAL-02 marked complete"
      contains: "[x] **QUAL-01**"
    - path: ".planning/PROJECT.md"
      provides: "Milestone-level Active/Validated buckets reconciled with actual completion state"
      contains: "Fix `rawResponse` type"
  key_links:
    - from: ".planning/REQUIREMENTS.md line 22"
      to: ".planning/REQUIREMENTS.md line 51 (traceability table)"
      via: "checkbox + table row pair must agree per QUAL-01"
      pattern: "QUAL-01.*Complete"
    - from: ".planning/REQUIREMENTS.md line 23"
      to: ".planning/REQUIREMENTS.md line 52 (traceability table)"
      via: "checkbox + table row pair must agree per QUAL-02"
      pattern: "QUAL-02.*Complete"
    - from: ".planning/PROJECT.md Active list (CR-01, WR-04, TYPO entries)"
      to: ".planning/PROJECT.md Validated list"
      via: "line move — remove from Active, insert into Validated in the same edit"
      pattern: "CR-01.*Phase 7"
---

<objective>
Reconcile formal requirement tracking documents with work already shipped in prior quick tasks and Phase 05.

Purpose: Close the tracking drift flagged by 07-RESEARCH.md Pitfall 4 — QUAL-01 (rawResponse type) and QUAL-02 (test runner consolidation) were completed in commits 9ba6e1d and fc9a507 (2026-07-28) but never marked complete in REQUIREMENTS.md or moved from PROJECT.md Active to Validated. Additionally, TYPO-01/02/03 were closed in REQUIREMENTS.md at Phase 05 completion but the corresponding "Designed PDF" bullets are still in PROJECT.md Active. Left uncorrected, Phase 7 verification and any future audit would flag these as open work when they are shipped.

Output: REQUIREMENTS.md and PROJECT.md updated to reflect real completion state. No source code changes. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/07-quality-packaging-global-install/07-RESEARCH.md
@.planning/phases/07-quality-packaging-global-install/07-PATTERNS.md

<interfaces>
<!-- Established checkbox and dating conventions the executor must match verbatim. -->

REQUIREMENTS.md completed-requirement format (from lines 11–13, TYPO-01/02/03):
```
- [x] **TYPO-01**: Designed PDF summary text renders at body-small size (visually smaller than body copy) — closed 2026-07-30
```
Rule: `- [x] **<ID>**: <original description text unchanged> — closed <YYYY-MM-DD>`

REQUIREMENTS.md traceability-table completed row format (from lines 49–51):
```
| TYPO-01 | Phase 5 | Complete — 2026-07-30 |
```

PROJECT.md Validated (v1.1) row format (from lines 48–49):
```
- [x] PDF output routed to `output/` or `output/<Company-Slug>/` relative to cwd based on interactive prompt — Phase 06 (2026-07-30)
```
Rule: `- [x] <description text> — Phase <NN> (<YYYY-MM-DD>)`

Dates to apply:
- QUAL-01: 2026-07-28 (commit 9ba6e1d)
- QUAL-02: 2026-07-28 (commit fc9a507)
- TYPO-01/02/03: 2026-07-30 (Phase 05 completion, per REQUIREMENTS.md lines 11–13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark QUAL-01 and QUAL-02 complete in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current checkbox state and traceability table)
    - .planning/phases/07-quality-packaging-global-install/07-PATTERNS.md (pattern for REQUIREMENTS.md analog, lines 107–133)
  </read_first>
  <action>
    Edit two regions of .planning/REQUIREMENTS.md.

    Region 1 — Quality &amp; Workflow checklist (currently lines 22–23):
    - Change the QUAL-01 line from an unchecked box to a checked box and append the closed suffix. Target line 22 output: `- [x] **QUAL-01**: \`rawResponse\` in \`ExtractResult\` is typed as \`ParsedMessage&lt;ResumeData&gt;\` so \`--verbose\` output is complete (CR-01) — closed 2026-07-28`
    - Change the QUAL-02 line from an unchecked box to a checked box and append the closed suffix. Target line 23 output: `- [x] **QUAL-02**: Test suite runs under a single test runner with no conflicting scripts in package.json (WR-04) — closed 2026-07-28`
    - Leave the QUAL-03 line unchanged (still `- [ ]`).

    Region 2 — Traceability table (currently lines 53–55):
    - Change the QUAL-01 row's Status column from `Pending` to `Complete — 2026-07-28`.
    - Change the QUAL-02 row's Status column from `Pending` to `Complete — 2026-07-28`.
    - Leave the QUAL-03 row unchanged (still `Pending`).

    Follow the existing `[x] ... — closed YYYY-MM-DD` and `Complete — YYYY-MM-DD` conventions established for TYPO-01/02/03 verbatim. Do NOT modify any other section (Coverage counts already reflect 8 total mapped requirements — that stays correct). Do NOT change the trailing "Last updated" line — that is updated at git commit time by the caller.
  </action>
  <verify>
    <automated>grep -Ec '^- \[x\] \*\*QUAL-0[12]\*\*.*— closed 2026-07-28' .planning/REQUIREMENTS.md | grep -q '^2$' &amp;&amp; grep -Ec '^\| QUAL-0[12] \| Phase 7 \| Complete — 2026-07-28 \|' .planning/REQUIREMENTS.md | grep -q '^2$' &amp;&amp; grep -qE '^- \[ \] \*\*QUAL-03\*\*' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly two lines in REQUIREMENTS.md match `^- \[x\] \*\*QUAL-0[12]\*\*.*— closed 2026-07-28`
    - Exactly two traceability rows in REQUIREMENTS.md match `^\| QUAL-0[12] \| Phase 7 \| Complete — 2026-07-28 \|`
    - The QUAL-03 checklist line still begins with `- [ ] **QUAL-03**` (unchanged)
    - The QUAL-03 traceability row still ends with `| Pending |`
    - No other requirement checkbox state changes (TYPO/OUTPUT rows unchanged)
    - The description text after each QUAL-01/QUAL-02 ID is identical to the pre-edit description (only the checkbox and trailing suffix change)
  </acceptance_criteria>
  <done>REQUIREMENTS.md reflects QUAL-01 and QUAL-02 as closed 2026-07-28 in both the checklist and traceability table; QUAL-03 remains pending; convention matches TYPO-01/02/03 exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Move CR-01, WR-04, and TYPO-01/02/03 from Active to Validated in PROJECT.md</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (current Active and Validated sections — verify exact line contents before editing)
    - .planning/phases/07-quality-packaging-global-install/07-PATTERNS.md (pattern for PROJECT.md analog, lines 136–169)
  </read_first>
  <action>
    Edit two adjacent sections of .planning/PROJECT.md: `### Validated (v1.1 — in progress)` and `### Active (v1.1)`.

    Remove from the Active (v1.1) section (currently lines 53–58) the following five bullets in their entirety:
    - `- [ ] Designed PDF: summary text scaled down to body-small size — v1.1`
    - `- [ ] Designed PDF: bullet points styled with subtle accent color (#2d4a6b) — v1.1`
    - `- [ ] Designed PDF: consistent bottom spacing under all section headers — v1.1`
    - `- [ ] Fix \`rawResponse\` type to \`ParsedMessage&lt;ResumeData&gt;\` for correct \`--verbose\` output (CR-01) — v1.1`
    - `- [ ] Remove vitest runner conflict — consolidate on one test runner (WR-04) — v1.1`

    Preserve in the Active (v1.1) section the one remaining bullet unchanged:
    - `- [ ] \`npm publish\` / \`npm link\` so \`cvgen\` can be run as a global command without \`npx tsx\` — v1.1`

    Append to the Validated (v1.1 — in progress) section (currently ends at line 49) the following five bullets in this exact order (following the two existing Phase 06 entries):
    - `- [x] Designed PDF: summary text scaled down to body-small size — Phase 05 (2026-07-30)`
    - `- [x] Designed PDF: bullet points styled with subtle accent color (#2d4a6b) — Phase 05 (2026-07-30)`
    - `- [x] Designed PDF: consistent bottom spacing under all section headers — Phase 05 (2026-07-30)`
    - `- [x] Fix \`rawResponse\` type to \`ParsedMessage&lt;ResumeData&gt;\` for correct \`--verbose\` output (CR-01) — Phase 7 (2026-07-28)`
    - `- [x] Remove vitest runner conflict — consolidate on one test runner (WR-04) — Phase 7 (2026-07-28)`

    Format rule (from PROJECT.md line 48–49 established convention): `- [x] <description unchanged> — Phase <NN> (<YYYY-MM-DD>)`. The description text after the checkbox must match the pre-edit Active description verbatim except the trailing ` — v1.1` marker is replaced with the Phase/date suffix.

    Do NOT modify: the Validated (v1.0) section, the Out of Scope section, the Context/Constraints/Key Decisions/Evolution sections, or the leading section headers.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] Designed PDF' .planning/PROJECT.md | grep -q '^0$' &amp;&amp; grep -c '^- \[x\] Designed PDF.*Phase 05 (2026-07-30)$' .planning/PROJECT.md | grep -q '^3$' &amp;&amp; grep -qE '^- \[x\] Fix `rawResponse` type.*CR-01.*Phase 7 \(2026-07-28\)$' .planning/PROJECT.md &amp;&amp; grep -qE '^- \[x\] Remove vitest runner conflict.*WR-04.*Phase 7 \(2026-07-28\)$' .planning/PROJECT.md &amp;&amp; ! grep -qE '^- \[ \] (Fix `rawResponse`|Remove vitest runner)' .planning/PROJECT.md &amp;&amp; grep -qE '^- \[ \] `npm publish`' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - Zero lines in PROJECT.md match `^- \[ \] Designed PDF` (all three Active TYPO entries removed)
    - Exactly three lines match `^- \[x\] Designed PDF.*Phase 05 \(2026-07-30\)$` (all three TYPO entries relocated to Validated)
    - Exactly one line matches `Fix .rawResponse. type.*CR-01.*Phase 7 \(2026-07-28\)` in the Validated section
    - Exactly one line matches `Remove vitest runner conflict.*WR-04.*Phase 7 \(2026-07-28\)` in the Validated section
    - Zero unchecked `- [ ]` bullets remain in PROJECT.md that reference `rawResponse`, `vitest runner`, or `Designed PDF`
    - The `npm publish` / `npm link` bullet still exists as `- [ ]` in the Active (v1.1) section (QUAL-03 still open)
    - The Validated (v1.1 — in progress) section retains the two pre-existing Phase 06 entries (routing + toCompanySlug)
  </acceptance_criteria>
  <done>PROJECT.md Active (v1.1) contains only the QUAL-03 global-install bullet; Validated (v1.1 — in progress) contains the two Phase 06 entries plus the three TYPO entries plus the CR-01 and WR-04 entries in the specified order; all move-target bullets use the established `[x] ... — Phase NN (YYYY-MM-DD)` format.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → planning docs | Human authoring markdown; no runtime consumer of these files |
| Git repo → GitHub public mirror | These files are public but contain no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | REQUIREMENTS.md checkbox state | accept | Files are planning artifacts, tracked in git; any accidental mis-edit is caught by grep-based automated verify commands in each task |
| T-07-02 | Information Disclosure | PROJECT.md / REQUIREMENTS.md prose | accept | Files are already public in the repository (github.com/patcartelli/cvgen); no PII or secrets involved |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs performed in this plan; no supply-chain surface |
</threat_model>

<verification>
Run automated verify commands from both tasks. All grep gates must return the counts specified.

Additionally, spot-check that the description text of each moved bullet is byte-identical to the pre-edit Active description (the only allowed diff is the checkbox state change and the trailing ` — v1.1` → ` — Phase NN (YYYY-MM-DD)` swap).
</verification>

<success_criteria>
1. REQUIREMENTS.md marks QUAL-01 and QUAL-02 complete (checkbox + traceability table) with date 2026-07-28
2. PROJECT.md Active (v1.1) contains only the QUAL-03 global-install entry
3. PROJECT.md Validated (v1.1 — in progress) contains all five migrated entries (three TYPO + CR-01 + WR-04) in the specified order with correct Phase/date suffixes
4. No other sections of either file are modified
</success_criteria>

<output>
Create `.planning/phases/07-quality-packaging-global-install/07-01-SUMMARY.md` when done, using the template at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
