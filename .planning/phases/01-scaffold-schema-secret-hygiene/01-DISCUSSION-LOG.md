# Phase 1: Scaffold, Schema & Secret Hygiene - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-27
**Phase:** 1-Scaffold, Schema & Secret Hygiene
**Areas discussed:** Obsidian note structure, Schema fields & depth, Secret scanning tooling

---

## Obsidian Note Structure

| Option | Description | Selected |
|--------|-------------|----------|
| YAML frontmatter | Contact info in Obsidian properties panel; section headings for the rest | ✓ |
| Pure headings — no frontmatter | # Name as first heading, contact info as paragraph | |
| Minimal frontmatter (name only) | Just name: in frontmatter, rest as paragraph | |

**User's choice:** YAML frontmatter for all contact fields

---

| Option | Description | Selected |
|--------|-------------|----------|
| ### Role at Company + dates line | Heading per role; date/location on next line; bullets below | ✓ |
| ### Company — role/dates as subtext | Company name leads; role and dates bold/pipe-separated on next line | |
| You decide | Claude picks the most parser-friendly convention | |

**User's choice:** `### Role at Company` with `Month Year – Month Year · Location` line + bullets

---

| Option | Description | Selected |
|--------|-------------|----------|
| Flat comma-separated list under ## Skills | All skills in one line | |
| Grouped categories with a heading per group | **Category:** items format | ✓ (corrected) |
| Inline within experience bullets | No separate Skills section | |

**User's choice:** Grouped by category — `**Category:** item1, item2` — initially selected flat list, then immediately corrected to grouped. Category name is free text (Design, AI/ML, Tools, etc.).

---

| Option | Description | Selected |
|--------|-------------|----------|
| No — five core sections only | Contact/Summary/Experience/Education/Skills | |
| Yes — Projects and/or Awards | Optional extra sections | |
| Open-ended — schema accepts any extra sections | Unknown sections passed through as freeform | |

**User's choice (freeform):** Professional Summary (optional), Core Competencies (ATS keyword list), no Projects section. Core Competencies is a manually-written flat keyword list targeting JD keywords for ATS — not generated by the CLI.

---

| Option | Description | Selected |
|--------|-------------|----------|
| ### Degree at Institution + year | Mirrors experience heading pattern | ✓ |
| Simple list items under ## Education | - UCLA — B.S. (2019) | |
| You decide | Claude picks | |

**User's choice:** `### Degree at Institution` + year on next line

---

| Option | Description | Selected |
|--------|-------------|----------|
| Month Year – Month Year | Human-friendly, e.g. Jan 2022 – Mar 2024 | ✓ |
| YYYY-MM – YYYY-MM | ISO format, easier to sort | |
| Free text — Claude normalizes | Write whatever, parser handles it | |

**User's choice:** Month Year – Month Year; current roles use "Present" (e.g. Jan 2022 – Present)

---

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — fixed order expected | Summary → Core Competencies → Experience → Education → Skills | ✓ |
| No — parser handles any order | Claude finds sections by heading name | |

**User's choice:** Fixed section ordering enforced

---

| Option | Description | Selected |
|--------|-------------|----------|
| name / email / phone / location / linkedin / github | All lowercase, standard Obsidian conventions | ✓ |
| Custom field names | User specifies different names | |

**User's choice:** `name / email / phone / location / linkedin / github` (all lowercase)

---

## Schema fields & depth

| Option | Description | Selected |
|--------|-------------|----------|
| Just the four basics | role, company, dates, bullets | |
| Add location per role | Plus location?: string | |
| Add location + employment type | Plus type?: 'full-time' \| 'contract' | ✓ (partial) |

**User's choice (freeform):** role / company / startDate / endDate / type? ('full-time' \| 'contract') / bullets. Clarified that "outcomes" = bullets — no separate field, same array.

---

| Option | Description | Selected |
|--------|-------------|----------|
| name + email required; rest optional | phone?, location?, linkedin?, github? | |
| All six required | Every frontmatter field required | ✓ |
| Only name required | Maximum flexibility | |

**User's choice:** All six contact fields required

---

| Option | Description | Selected |
|--------|-------------|----------|
| Any string for category name | category: string, items: string[] | ✓ |
| Enum of allowed categories | Fixed set of category names | |

**User's choice:** Any string for category — schema stays flexible

---

## Secret scanning tooling

| Option | Description | Selected |
|--------|-------------|----------|
| gitleaks | Go binary, 150+ built-in secret patterns, zero runtime deps | ✓ |
| detect-secrets | Yelp's Python tool, generates baseline file | |
| git-secrets | AWS tool, needs custom patterns for Anthropic keys | |

**User's choice:** gitleaks

---

| Option | Description | Selected |
|--------|-------------|----------|
| simple-git-hooks | Config in package.json, minimal setup | ✓ |
| Husky + lint-staged | Industry standard, more dependencies | |
| Lefthook | YAML config, parallel execution | |

**User's choice:** simple-git-hooks

---

## Claude's Discretion

- Exact `tsconfig.json` compiler options (flags beyond strict mode and NodeNext resolution)
- gitleaks `.gitleaks.toml` config if custom patterns needed beyond built-in Anthropic ruleset
- File/directory naming for `src/lib/`, `src/cli.ts`, `fixtures/`

## Deferred Ideas

None — discussion stayed within phase scope.
