---
phase: 02-markdown-to-structured-json-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/extract.ts
  - src/lib/preflight.ts
  - src/schema/validate.ts
  - fixtures/sample-resume.md
autonomous: true
requirements: [PARSE-01, PARSE-02, PARSE-03]

must_haves:
  truths:
    - "extractResume(markdown) returns a schema-validated ResumeData object when Claude returns valid structured output"
    - "extractResume throws (never process.exit) if Claude returns null parsed_output"
    - "preflightCheck(markdown) returns an array naming each missing frontmatter field or section heading, with no false positives on the clean fixture"
    - "validateResume produces path-based Zod error messages of shape `{field.path}: {message}` per issue when validation fails"
    - "fixtures/sample-resume.md is well-formed per D-01..D-08 and represents the same Alex Rivera persona as sample-resume.json"
  artifacts:
    - path: "src/lib/extract.ts"
      provides: "extractResume(markdown: string): Promise<ResumeData>"
      exports: ["extractResume"]
      min_lines: 15
    - path: "src/lib/preflight.ts"
      provides: "preflightCheck(markdown: string): PreflightError[]"
      exports: ["preflightCheck", "PreflightError"]
      min_lines: 30
    - path: "src/schema/validate.ts"
      provides: "validateResume with path-based Zod error formatting"
      exports: ["validateResume"]
      contains: "issue.path"
    - path: "fixtures/sample-resume.md"
      provides: "Well-formed markdown fixture matching sample-resume.json"
      contains: "## Experience"
  key_links:
    - from: "src/lib/extract.ts"
      to: "src/schema/resume.ts"
      via: "import { ResumeSchema, type ResumeData }"
      pattern: "ResumeSchema.*from.*schema/resume\\.js"
    - from: "src/lib/extract.ts"
      to: "@anthropic-ai/sdk"
      via: "messages.parse + zodOutputFormat"
      pattern: "messages\\.parse|zodOutputFormat"
    - from: "src/schema/validate.ts"
      to: "src/schema/resume.ts"
      via: "safeParse on ResumeSchema"
      pattern: "ResumeSchema\\.safeParse"
---

<objective>
Build the pure library layer for Phase 2 extraction: the Claude API wrapper (extractResume),
the pre-flight markdown validator (preflightCheck), the upgraded post-call Zod formatter
(validateResume), and the happy-path markdown fixture. These are the reusable, no-I/O
functions Phase 4 will import and the CLI entry (Plan 02) will orchestrate.

Purpose: Isolate all extraction/validation logic in importable functions before wiring
any CLI concerns. No process.exit, no console.log in this plan — those are Plan 02's job.

Output: 3 new/modified TypeScript files + 1 markdown fixture. `tsc --noEmit` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-to-structured-json-extraction/02-CONTEXT.md
@.planning/phases/02-markdown-to-structured-json-extraction/02-RESEARCH.md
@.planning/phases/02-markdown-to-structured-json-extraction/02-PATTERNS.md

# Existing source-of-truth files
@src/schema/resume.ts
@src/schema/validate.ts
@fixtures/sample-resume.json
@scripts/validate-fixtures.ts
@tsconfig.json
@package.json

<interfaces>
<!-- Existing exports the executor will consume. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From src/schema/resume.ts:
- export const ResumeSchema = z.object({ contact, summary?, coreCompetencies?, experience[], education[], skills[] })
- export type ResumeData = z.infer<typeof ResumeSchema>
- ContactSchema fields (required): name, email, phone, location, linkedin, github
- ExperienceSchema fields: role, company, startDate, endDate?, type? ("full-time"|"contract"), bullets[]
- EducationSchema fields: degree, institution, year
- SkillGroupSchema fields: category, items[]

From node_modules/@anthropic-ai/sdk (per RESEARCH.md Pattern 1, VERIFIED):
- default export: Anthropic (client class; reads process.env.ANTHROPIC_API_KEY at construction)
- from "@anthropic-ai/sdk/helpers/zod.js": zodOutputFormat(schema) → AutoParseableOutputFormat<T>
- client.messages.parse({ model, max_tokens, system, messages, output_config: { format } })
  returns ParsedMessage<T> with parsed_output: T | null

From zod@4.4.3 (per RESEARCH.md Pattern 2, VERIFIED):
- ResumeSchema.safeParse(data) → { success: true, data } | { success: false, error }
- error.issues: Array<{ path: PropertyKey[]; message: string; ... }>
- Import from bare "zod" (already established in src/schema/resume.ts)

Existing patterns to preserve (from PATTERNS.md Shared Patterns):
- ESM: all relative imports use .js extension
- node:fs/promises, node:path prefixes for built-ins
- verbatimModuleSyntax=true → split `import type` from value imports on separate lines
- Library files throw Error; never call process.exit()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade validateResume with path-based Zod error formatting (PARSE-02, PARSE-03)</name>
  <files>src/schema/validate.ts, src/schema/validate.test.ts</files>
  <read_first>
    - src/schema/validate.ts (current Phase 1 stub — starting point)
    - src/schema/resume.ts (ResumeSchema definition — issue paths reference these fields)
    - fixtures/sample-resume.json (valid data — must pass)
    - fixtures/sample-resume-malformed.json (invalid data — must fail with formatted errors)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-PATTERNS.md (validate.ts upgrade pattern, lines 171–219)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-RESEARCH.md (Pattern 2, lines 219–250)
    - scripts/validate-fixtures.ts (existing safeParse + fixture usage pattern)
    - tsconfig.json (verbatimModuleSyntax=true → separate type/value imports)
  </read_first>
  <behavior>
    - Test 1: validateResume(sampleResumeJson) returns the same object (typed as ResumeData) with no throw
    - Test 2: validateResume(sampleResumeMalformedJson) throws Error whose message starts with "Resume validation failed:" and contains at least one line of shape "  {path}: {message}"
    - Test 3: validateResume(sampleResumeMalformedJson) error message includes the specific field path where the malformed fixture is broken (verify by inspecting the malformed fixture's actual defect)
    - Test 4: validateResume({}) throws Error naming top-level required fields (e.g. "contact") in the path
    - Test 5: A numeric path segment renders as "[N]" (e.g. "experience[0].bullets") not ".0"; a string segment renders with a leading "." except at the start
  </behavior>
  <action>
    Upgrade src/schema/validate.ts (per D-E03) to replace the generic `result.error.message` throw with a private `formatZodErrors(issues)` helper that maps each issue to `  {joined-path}: {message}` and joins with newlines. Path join rule: numeric segments render as `[N]`, string segments render with a leading `.` except when they are the first segment (first string segment has no leading dot). Empty path renders as `(root)`. Preserve the existing named export `validateResume`, the split `import type { ResumeData }` + `import { ResumeSchema }` from "./resume.js", and the safeParse+result.data return path. The thrown Error message MUST start with `Resume validation failed:\n` followed by the formatted issues block.

    Create src/schema/validate.test.ts as a plain `node --test`-compatible TypeScript test that can be run via `tsx --test src/schema/validate.test.ts`. Tests must load both fixtures via `readFile` from `node:fs/promises` + `resolve` from `node:path` (relative to project root; use `import.meta.url` + `fileURLToPath` pattern from scripts/validate-fixtures.ts). Assert the five behaviors above using `node:assert/strict`. Do NOT depend on the exact Zod message text — assert the structural shape (starts-with, contains, regex match on `/\[\d+\]|\w+/`).

    Do not modify src/schema/resume.ts. Do not import from "zod/v4" — infer issue shape from `result.error.issues` (already `$ZodIssue[]`).
  </action>
  <verify>
    <automated>cd /Users/pcartelli/dev/cvgen && npx tsx --test src/schema/validate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsx --test src/schema/validate.test.ts` exits 0 with all 5 tests passing
    - `grep -n "issue.path" src/schema/validate.ts` returns at least one match
    - `grep -n "Resume validation failed:" src/schema/validate.ts` returns exactly one match
    - `grep -c "process.exit" src/schema/validate.ts` returns 0 (library file, must not exit)
    - `grep -n "from \"zod/v4\"" src/schema/validate.ts` returns 0 matches (must use bare "zod")
    - `npm run typecheck` exits 0
    - src/schema/validate.ts still exports named `validateResume` with signature `(data: unknown) => ResumeData`
  </acceptance_criteria>
  <done>
    validateResume produces path-based errors like `experience[0].bullets: Required` when validation fails on the malformed fixture; still returns typed ResumeData on the valid fixture; test file locks the behavior; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create preflightCheck for frontmatter + section headings (PARSE-03, per D-E02)</name>
  <files>src/lib/preflight.ts, src/lib/preflight.test.ts</files>
  <read_first>
    - .planning/phases/02-markdown-to-structured-json-extraction/02-CONTEXT.md (D-E01, D-E02, D-E03 decisions; specifics section for error message wording)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-RESEARCH.md (Pattern 3, lines 253–306; Pitfall 6, lines 423–428)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-PATTERNS.md (preflight.ts section, lines 96–169)
    - src/schema/resume.ts (to cross-check required frontmatter field list matches ContactSchema required fields)
    - tsconfig.json (verbatimModuleSyntax; noUncheckedIndexedAccess=true)
  </read_first>
  <behavior>
    - Test 1: preflightCheck on a string containing the six required frontmatter fields (`name:`, `email:`, `phone:`, `location:`, `linkedin:`, `github:`) inside a `---` block AND the three required headings (`## Experience`, `## Education`, `## Skills`) returns []
    - Test 2: preflightCheck on empty string returns errors for all 6 fields AND all 3 sections (9 total) — order-agnostic
    - Test 3: preflightCheck on a string with frontmatter missing only `linkedin` returns exactly one error with `type: "frontmatter"`, `missing: "linkedin"`, `message: "Missing required frontmatter field: linkedin"`
    - Test 4: preflightCheck on a string with all frontmatter but missing `## Education` returns exactly one error with `type: "section"`, `missing: "## Education"`, `message: "Missing required section: ## Education"`
    - Test 5: preflightCheck does NOT flag optional sections (`## Professional Summary`, `## Core Competencies`) as missing when they are absent (per D-E02 — summary and Core Competencies are optional)
    - Test 6: preflightCheck detects a heading at the very start of the document (no leading newline) — e.g. document beginning `## Experience\n...`
    - Test 7: Frontmatter regex handles trailing content — a `---` horizontal rule appearing later in the body does not truncate the frontmatter check (the non-greedy `^---\n([\s\S]*?)\n---` anchored at start pattern stops at the first closing `---`)
  </behavior>
  <action>
    Create src/lib/preflight.ts with:
    - `const REQUIRED_FRONTMATTER_FIELDS = ["name", "email", "phone", "location", "linkedin", "github"] as const` (order and casing must match ContactSchema required fields in src/schema/resume.ts)
    - `const REQUIRED_SECTION_HEADINGS = ["## Experience", "## Education", "## Skills"] as const` (per D-E02 — do NOT include `## Professional Summary` or `## Core Competencies`)
    - `export interface PreflightError { type: "frontmatter" | "section"; missing: string; message: string; }`
    - `export function preflightCheck(markdown: string): PreflightError[]` — accumulates ALL errors (does not fail-fast; per Claude's Discretion note in RESEARCH.md, accumulating gives the user a complete picture in one run). Uses `markdown.match(/^---\n([\s\S]*?)\n---/)` to extract frontmatter (falls back to empty string if no match — then all 6 field checks fail, which is correct). For each required field, tests with `new RegExp(\`^${field}:\`, "m")` against the extracted frontmatter block. For each required heading, checks `markdown.includes(\`\\n${heading}\`) || markdown.startsWith(heading)` (per Task 6 above — heading at start of document must also match).
    - Error messages MUST use the exact wording from the CONTEXT specifics: `Missing required frontmatter field: {field}` and `Missing required section: {heading}` (heading string includes the leading `## ` prefix).
    - NO imports required (pure string operations). NO process.exit. NO console.log.

    Create src/lib/preflight.test.ts using `node --test` compatible tests (invoked via `tsx --test`). Build the test markdown strings inline as string literals — do not depend on the fixture file (that keeps preflight tests independent of Task 3). Assert the 7 behaviors above with `node:assert/strict`.
  </action>
  <verify>
    <automated>cd /Users/pcartelli/dev/cvgen && npx tsx --test src/lib/preflight.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsx --test src/lib/preflight.test.ts` exits 0 with all 7 tests passing
    - `grep -c "process.exit" src/lib/preflight.ts` returns 0
    - `grep -c "console\." src/lib/preflight.ts` returns 0 (pure function, no logging)
    - `grep -c "^import" src/lib/preflight.ts` returns 0 (no imports needed for the implementation itself; a `type`-only self-import is not required)
    - `grep -c "Missing required frontmatter field:" src/lib/preflight.ts` returns exactly 1
    - `grep -c "Missing required section:" src/lib/preflight.ts` returns exactly 1
    - `grep -c "REQUIRED_FRONTMATTER_FIELDS" src/lib/preflight.ts` returns at least 2 (declaration + usage)
    - `npm run typecheck` exits 0
    - Exports match: `preflightCheck` function AND `PreflightError` interface named
  </acceptance_criteria>
  <done>
    preflightCheck accumulates missing-frontmatter and missing-section errors with human-readable messages naming the specific missing item; optional sections are not flagged; test file locks the seven behaviors; typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create extractResume Claude API wrapper + happy-path markdown fixture (PARSE-01, per D-P01/D-P02/D-M01/D-F01)</name>
  <files>src/lib/extract.ts, fixtures/sample-resume.md</files>
  <read_first>
    - .planning/phases/02-markdown-to-structured-json-extraction/02-CONTEXT.md (D-P01, D-P02, D-M01, D-F01)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-RESEARCH.md (Pattern 1 lines 185–217; Pattern 6 model selection lines 345–355; Anti-Patterns lines 357–363; Pitfalls 1, 2, 4, 5 lines 380–419)
    - .planning/phases/02-markdown-to-structured-json-extraction/02-PATTERNS.md (extract.ts section lines 23–93; fixtures/sample-resume.md section lines 326–421)
    - src/schema/resume.ts (target schema shape; extract.ts imports both value and type)
    - fixtures/sample-resume.json (ground-truth data — the .md fixture must represent the same persona/data)
    - .planning/phases/01-scaffold-schema-secret-hygiene/01-CONTEXT.md (D-01 through D-08 — locked markdown convention)
    - package.json (confirm @anthropic-ai/sdk@0.115.0 installed; no new deps)
    - tsconfig.json (verbatimModuleSyntax, NodeNext resolution → .js on subpath imports)
  </read_first>
  <behavior>
    - Test 1 (type-check only, no live API call): The exported `extractResume` has TypeScript signature `(markdown: string) => Promise<ResumeData>`. Verified indirectly by `tsc --noEmit`.
    - Test 2 (structural): src/lib/extract.ts uses `client.messages.parse` (not `messages.create`), passes `output_config: { format: zodOutputFormat(ResumeSchema) }`, uses model `"claude-haiku-4-5"`, uses `max_tokens: 2048`, and the system prompt is a single sentence (per D-P01: schema-only, minimal — MUST NOT include D-01..D-08 heading conventions).
    - Test 3 (structural): extract.ts imports `zodOutputFormat` from `"@anthropic-ai/sdk/helpers/zod.js"` (with `.js` extension per NodeNext).
    - Test 4 (structural): extract.ts throws (does not `process.exit`) when `response.parsed_output === null`.
    - Test 5 (fixture structural): fixtures/sample-resume.md has a `---`-delimited frontmatter block containing all 6 required fields with values from sample-resume.json (name: Alex Rivera, email: alex@example.com, phone: (555) 000-0000, location: San Francisco, CA, linkedin: linkedin.com/in/alexrivera, github: github.com/alexrivera).
    - Test 6 (fixture structural): fixtures/sample-resume.md contains section headings `## Experience`, `## Education`, `## Skills` (required per D-E02) and follows section order Summary → Core Competencies → Experience → Education → Skills (D-08).
    - Test 7 (fixture semantic): preflightCheck(readFileSync(fixtures/sample-resume.md)) returns [] — the fixture passes preflight with no errors. This ties the fixture to Task 2's contract.
  </behavior>
  <action>
    Create src/lib/extract.ts implementing the `extractResume` function per D-M01 signature `extractResume(markdown: string): Promise<ResumeData>`:
    - Imports (in this order, on separate lines): default `Anthropic` from `"@anthropic-ai/sdk"`; `zodOutputFormat` from `"@anthropic-ai/sdk/helpers/zod.js"` (MANDATORY `.js` per Pitfall 2); `type { ResumeData }` from `"../schema/resume.js"`; value `{ ResumeSchema }` from `"../schema/resume.js"` (split per verbatimModuleSyntax).
    - Instantiate client with `new Anthropic()` inside the function (SDK reads ANTHROPIC_API_KEY from process.env at construction — do NOT pass the key explicitly, per SEC-01).
    - Call `await client.messages.parse({ model: "claude-haiku-4-5", max_tokens: 2048, system: "<one-sentence schema-only prompt>", messages: [{ role: "user", content: markdown }], output_config: { format: zodOutputFormat(ResumeSchema) } })`. Pass the object literal inline (do not destructure — per Pitfall 1).
    - The system prompt MUST be a single short sentence that does NOT reference D-01..D-08 heading conventions (per D-P01). Suggested wording: `"Extract the resume data from this markdown document."` — Claude's Discretion allows minor variation but keep it schema-only and minimal.
    - After the call, guard `if (response.parsed_output === null) throw new Error("Claude returned a response that could not be parsed as structured resume data.")` (Pitfall 4).
    - Return `response.parsed_output` — its type is `ResumeData` (inferred from zodOutputFormat generic).
    - MUST NOT call `process.exit`. MUST NOT `console.log`. MUST NOT preprocess the markdown string before sending (per D-P02 — pass raw markdown as the user message content).

    Create fixtures/sample-resume.md matching the D-01..D-08 convention and representing the same Alex Rivera data as fixtures/sample-resume.json. Use the exact markdown template shown in 02-PATTERNS.md lines 378–411 (Alex Rivera fixture). Frontmatter uses lowercase unquoted values. Experience heading is `### Role at Company`. Date line is `Month Year – Present · Location` or `Month Year – Month Year · Location`. Skills use `**Category:** item1, item2` format. Section order MUST be Summary → Core Competencies → Experience → Education → Skills.

    Add a small structural test file src/lib/extract.test.ts using `node --test` that asserts Tests 5, 6, 7 above (fixture structural + preflight-passes) by reading fixtures/sample-resume.md via `node:fs/promises` and calling `preflightCheck` from `./preflight.js`. Do NOT make a live API call in this test. Tests 2, 3, 4 (structural assertions about extract.ts source) can be asserted by reading src/lib/extract.ts as text and running regex/substring checks — this is legitimate because it locks the contract shape without needing an API key.

    Do NOT add a new dependency to package.json — `@anthropic-ai/sdk@0.115.0` and `zod@4.4.3` are already installed (verified in package.json dependencies).
  </action>
  <verify>
    <automated>cd /Users/pcartelli/dev/cvgen && npm run typecheck && npx tsx --test src/lib/extract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` exits 0
    - `tsx --test src/lib/extract.test.ts` exits 0 with all fixture + structural tests passing
    - `grep -c "messages.parse" src/lib/extract.ts` returns exactly 1
    - `grep -c "messages.create" src/lib/extract.ts` returns 0 (anti-pattern; must use parse)
    - `grep -c "@anthropic-ai/sdk/helpers/zod.js" src/lib/extract.ts` returns exactly 1 (with .js extension per Pitfall 2)
    - `grep -c "process.exit" src/lib/extract.ts` returns 0 (library file)
    - `grep -c "claude-haiku-4-5" src/lib/extract.ts` returns exactly 1
    - `grep -c "max_tokens" src/lib/extract.ts` returns exactly 1 (value 2048)
    - `grep -c "parsed_output === null" src/lib/extract.ts` returns at least 1 (guard present)
    - `grep -E "^(name|email|phone|location|linkedin|github):" fixtures/sample-resume.md | wc -l` returns 6
    - `grep -c "^## Experience$" fixtures/sample-resume.md` returns 1
    - `grep -c "^## Education$" fixtures/sample-resume.md` returns 1
    - `grep -c "^## Skills$" fixtures/sample-resume.md` returns 1
    - `grep -c "Alex Rivera" fixtures/sample-resume.md` returns at least 1 (matches sample-resume.json persona)
    - The system prompt string in src/lib/extract.ts does NOT include the substring "Experience" or "###" (per D-P01 — no D-01..D-08 heading references)
  </acceptance_criteria>
  <done>
    extractResume is a pure library function that calls Claude's messages.parse with zodOutputFormat(ResumeSchema) and returns validated ResumeData; the happy-path markdown fixture matches sample-resume.json and passes preflightCheck; source-level and fixture-level tests lock the contract without needing a live API key.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown file → extractResume | User-provided markdown (their own resume) crosses into the API request body |
| ANTHROPIC_API_KEY env var → Anthropic client | Secret loaded from process.env at client construction |
| Claude API response → ResumeData return | Untrusted upstream response validated by zodOutputFormat + ResumeSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Claude API response → return value in src/lib/extract.ts | mitigate | zodOutputFormat(ResumeSchema) validates response server-side via SDK; extractResume guards `parsed_output === null` and throws rather than returning malformed data. Downstream consumers receive typed ResumeData or an Error, never garbage. |
| T-02-02 | Information disclosure | ANTHROPIC_API_KEY handling in src/lib/extract.ts | mitigate | Client is constructed with `new Anthropic()` (no explicit key argument); SDK reads process.env at construction. extract.ts NEVER logs, prints, or serializes the key value. No `console.log(process.env.ANTHROPIC_API_KEY)` or equivalent. |
| T-02-03 | Tampering | User-provided markdown → Claude prompt (prompt injection surface) | accept | Resume is the user's own document (single-user CLI); output is schema-constrained (zodOutputFormat rejects out-of-schema responses); no downstream code execution based on Claude output. Content-invention/rewriting is explicitly out-of-scope per REQUIREMENTS.md "Out of Scope". Risk is low-value and residual. |
| T-02-04 | Information disclosure | Test fixtures | mitigate | fixtures/sample-resume.md uses fictional persona "Alex Rivera" with example.com email and (555) 000-0000 phone (matching existing sample-resume.json). No real PII enters the repository. Fixture is committed to public repo — verified fictional. |
| T-02-SC | Tampering | npm installs (no new deps) | mitigate | No new dependencies added this plan — @anthropic-ai/sdk@0.115.0 and zod@4.4.3 are already installed and were audited in Phase 2 Package Legitimacy Audit (RESEARCH.md lines 111–122: both Approved). No slopcheck checkpoint needed. |
</threat_model>

<verification>
## Overall Plan Verification

Run in order:

1. `cd /Users/pcartelli/dev/cvgen && npm run typecheck` — exits 0
2. `npx tsx --test src/schema/validate.test.ts` — 5 tests pass
3. `npx tsx --test src/lib/preflight.test.ts` — 7 tests pass
4. `npx tsx --test src/lib/extract.test.ts` — fixture + structural tests pass
5. `npm run lint` — biome reports 0 errors
6. `grep -c "process.exit" src/lib/extract.ts src/lib/preflight.ts src/schema/validate.ts` returns `0` for each library file (grep prints per-file counts)

## Requirement Coverage Check

- PARSE-01: `grep -c "messages.parse" src/lib/extract.ts` = 1
- PARSE-02: `grep -c "safeParse" src/schema/validate.ts` >= 1
- PARSE-03: `grep -c "Missing required" src/lib/preflight.ts` = 2 AND `grep -c "issue.path" src/schema/validate.ts` >= 1
</verification>

<success_criteria>
- Every artifact in must_haves.artifacts exists at its stated path with the stated export
- Every key_link's pattern is grep-detectable in the stated file
- All three test files pass under `tsx --test`
- `npm run typecheck` clean
- No library file contains `process.exit`
- fixtures/sample-resume.md preflightCheck returns []
- Ready for Plan 02 (CLI wiring) to import `extractResume`, `preflightCheck`, `PreflightError`, `validateResume`
</success_criteria>

<output>
Create `.planning/phases/02-markdown-to-structured-json-extraction/02-01-SUMMARY.md` when done, following the standard summary template. Include: files created/modified, test counts, any deviations from the plan, and the exact exported signatures other plans/phases will consume.
</output>
