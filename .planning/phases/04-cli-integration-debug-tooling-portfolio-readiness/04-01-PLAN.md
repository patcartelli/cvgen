---
phase: "04-cli-integration-debug-tooling-portfolio-readiness"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/extract.ts
  - src/lib/extract.test.ts
autonomous: true
requirements:
  - DEVX-02
must_haves:
  truths:
    - "`commander` appears in package.json dependencies and is present in node_modules"
    - "`extractResume` returns `{ data: ResumeData; rawResponse: Message }` (ExtractResult), not bare ResumeData"
    - "All existing extract.ts tests still pass after the return type change"
  artifacts:
    - path: "package.json"
      provides: "commander dependency declaration"
      contains: '"commander"'
    - path: "src/lib/extract.ts"
      provides: "ExtractResult interface + updated return type"
      exports: ["ExtractResult", "extractResume"]
  key_links:
    - from: "src/lib/extract.ts"
      to: "src/cli/index.ts (Plan 02)"
      via: "ExtractResult.rawResponse used by --verbose flag"
      pattern: "ExtractResult"
---

<objective>
Install the Commander 15 dependency and update `extractResume` to return both the parsed resume data and the raw Anthropic SDK response. This plan makes the two changes that must exist before the CLI replacement in Plan 02 can compile.

Purpose: Commander must be in node_modules before any import from 'commander' works; and the CLI needs access to the raw API response for --verbose output, which requires the extract layer to expose it.

Output: `package.json` with `commander@^15.0.0` in dependencies, `node_modules/commander` installed, `src/lib/extract.ts` returning `ExtractResult`, and `extract.test.ts` updated to confirm the new interface without breaking existing assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-integration-debug-tooling-portfolio-readiness/04-RESEARCH.md
@.planning/phases/04-cli-integration-debug-tooling-portfolio-readiness/04-PATTERNS.md
</context>

<interfaces>
From src/lib/extract.ts (current — to be changed):

```typescript
// Current signature (to be replaced):
export async function extractResume(markdown: string): Promise<ResumeData>

// Current return statement (to be replaced):
return response.parsed_output;

// Current imports (preserve all; add Message type import):
import Anthropic from "@anthropic-ai/sdk";
import { zodOutputFormat } from "@anthropic-ai/sdk/helpers/zod.js";
import type { ResumeData } from "../schema/resume.js";
import { ResumeSchema } from "../schema/resume.js";
```

From src/lib/extract.test.ts (current — Tests 2–7 are source-level/fixture assertions; none call extractResume at runtime):
- Test 2: asserts messages.parse, zodOutputFormat, model, max_tokens
- Test 3: asserts import path for zodOutputFormat
- Test 4: asserts parsed_output null guard, no process.exit
- Tests 5–7: fixture-level assertions, unaffected

From package.json (current dependencies block):
```json
"dependencies": {
  "@anthropic-ai/sdk": "^0.115.0",
  "puppeteer": "^25.4.0",
  "zod": "^4.4.3"
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install Commander 15 and declare dependency in package.json</name>
  <files>package.json</files>
  <read_first>
    /Users/pcartelli/dev/cvgen/package.json
  </read_first>
  <action>
    Run `npm install commander` in the project root. This installs `commander@15.0.0` (the latest 15.x, per RESEARCH.md) and adds it to `dependencies` in package.json. Commander 15 ships its own TypeScript typings at `typings/index.d.ts` — no `@types/commander` is needed or should be added.

    After `npm install commander` completes, verify the resulting `package.json` `dependencies` block contains `"commander": "^15.0.0"` (the caret range is correct; do not pin to an exact version). The `bin`, `type`, `scripts`, and `devDependencies` fields must remain unchanged.

    Do not add `allowScripts` for commander — it has no postinstall scripts that require allow-listing.
  </action>
  <verify>
    <automated>node -e "import('commander').then(m => console.log('commander ok:', typeof m.Command))"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `dependencies` contains `"commander": "^15.0.0"`
    - `node_modules/commander` directory exists
    - `node -e "import('commander').then(m => console.log(typeof m.Command))"` prints `function`
    - `npm test` still exits 0 (existing tests unaffected by the install)
  </acceptance_criteria>
  <done>Commander 15 is importable in the project and declared in package.json dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update extractResume return type to ExtractResult and add confirming source assertion to extract.test.ts</name>
  <files>src/lib/extract.ts, src/lib/extract.test.ts</files>
  <read_first>
    /Users/pcartelli/dev/cvgen/src/lib/extract.ts
    /Users/pcartelli/dev/cvgen/src/lib/extract.test.ts
  </read_first>
  <behavior>
    - `extractResume` return type changes from `Promise<ResumeData>` to `Promise<ExtractResult>`
    - `ExtractResult` interface is exported: `{ data: ResumeData; rawResponse: Message }`
    - The null guard (`if (response.parsed_output === null)`) is preserved verbatim
    - The return statement changes from `return response.parsed_output` to `return { data: response.parsed_output, rawResponse: response }`
    - A new Test 8 in extract.test.ts asserts `ExtractResult` is exported and `rawResponse` appears in the source
    - All existing Tests 2–7 in extract.test.ts continue to pass unchanged
  </behavior>
  <action>
    Edit `src/lib/extract.ts` to make exactly four changes and nothing else:

    1. Add a `Message` type import after the existing imports. The import path to verify against installed SDK typings is `@anthropic-ai/sdk/resources/messages.js`. If that path does not resolve, fall back to `@anthropic-ai/sdk` (the SDK re-exports `Message` from its root). Check with `node -e "import('@anthropic-ai/sdk/resources/messages.js').then(m => console.log(typeof m))"` before committing to the subpath.

    2. Add the `ExtractResult` interface as an exported interface before the `extractResume` function:
       `export interface ExtractResult { data: ResumeData; rawResponse: Message; }`

    3. Change the function signature return type from `Promise<ResumeData>` to `Promise<ExtractResult>`.

    4. Change the return statement from `return response.parsed_output;` to `return { data: response.parsed_output, rawResponse: response };`.

    Do not change anything else in the file — the model, max_tokens, system prompt, null guard, and all imports except the one addition remain verbatim.

    Then edit `src/lib/extract.test.ts` to add a new Test 8 as the last test inside the `describe` block. Model it on the existing source-level assertion pattern (read the file synchronously or asynchronously — either is fine, existing tests use both). The new test must assert:
    - `src.includes("ExtractResult")` — interface is exported
    - `src.includes("rawResponse")` — the new field appears in the return
    - `src.includes("data: response.parsed_output")` — the data field is set from parsed_output
    Do NOT add any runtime call to `extractResume` — keep all tests as source-level or fixture assertions, matching the existing test file's philosophy.
  </action>
  <verify>
    <automated>npx tsx --test src/lib/extract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/extract.ts` contains `export interface ExtractResult`
    - `src/lib/extract.ts` function signature is `Promise<ExtractResult>`
    - `src/lib/extract.ts` return statement is `return { data: response.parsed_output, rawResponse: response };`
    - `src/lib/extract.ts` null guard `if (response.parsed_output === null)` is preserved verbatim
    - `npx tsx --test src/lib/extract.test.ts` exits 0 with all 7 existing tests plus new Test 8 passing
    - `npx tsc --noEmit` exits 0 (type-checks cleanly with the new interface)
  </acceptance_criteria>
  <done>extractResume returns ExtractResult; all 8 extract tests pass; tsc type-checks cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | `npm install commander` pulls from the public registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `npm install commander` | mitigate | Package Legitimacy Audit in RESEARCH.md confirms commander is [VERIFIED: official CLAUDE.md + npm registry, 15-year history, ~50M weekly downloads]. No blocking checkpoint required. |
| T-04-SC | Tampering | npm install | accept | commander is explicitly named in CLAUDE.md recommended stack and has a verified 15-year provenance — treated as [VERIFIED]. No [SUS] or [ASSUMED] packages in this plan. |
| T-04-02 | Information Disclosure | `extractResume` rawResponse | mitigate | `rawResponse` (the full SDK Message object) must never be printed to stdout or logged in non-verbose paths. CLI in Plan 02 gates this behind `options.verbose`. The extract layer itself never prints — it only returns. |
</threat_model>

<verification>
After both tasks complete:
1. `node -e "import('commander').then(m => console.log(typeof m.Command))"` prints `function`
2. `npx tsx --test src/lib/extract.test.ts` exits 0, all 8 tests pass
3. `npx tsc --noEmit` exits 0
4. `npm test` exits 0 (all existing tests in the project still pass)
</verification>

<success_criteria>
- `commander` installed and importable
- `ExtractResult` interface exported from `src/lib/extract.ts` with `data: ResumeData` and `rawResponse: Message` fields
- All extract tests pass including new Test 8 asserting the new interface
- Project type-checks cleanly
</success_criteria>

<output>
Create `.planning/phases/04-cli-integration-debug-tooling-portfolio-readiness/04-01-SUMMARY.md` when done.
</output>
