---
phase: 03-designed-ats-pdf-rendering
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/render.test.ts
  - tsconfig.test.json
autonomous: false
requirements:
  - RENDER-02
  - RENDER-03

must_haves:
  truths:
    - "Running `npx tsx --test src/lib/render.test.ts` exits 0 with all tests passing"
    - "The ATS PDF text extracted by pdf-parse contains every top-level content field from fixtures/sample-resume.json (name, email, phone, location, linkedin, github, summary, every experience role/company, every education entry, every skill category)"
    - "The extracted ATS text confirms linear reading order: name appears before email; contact block appears before experience; experience appears before education; education appears before skills"
    - "resolveOutputPaths returns paths in the same directory as the input .md path and uses the correct `-resume.pdf` / `-resume-ats.pdf` suffix pattern"
    - "The human reviewer confirms via a blocking checkpoint that the designed PDF renders with Inter, a muted accent color on the candidate name/section headers, single-column layout, no images, and no orphaned section headers (D-T02)"
  artifacts:
    - path: "src/lib/render.test.ts"
      provides: "node:test suite: pdf-parse ATS extraction assertions + resolveOutputPaths unit tests"
      min_lines: 100
    - path: "tsconfig.test.json"
      provides: "Test-scope TypeScript config already includes src/lib/*.test.ts; verify render.test.ts is picked up"
  key_links:
    - from: "src/lib/render.test.ts"
      to: "src/lib/render.ts"
      via: "import { renderAts, resolveOutputPaths }"
      pattern: "from \"\\./render\\.js\""
    - from: "src/lib/render.test.ts"
      to: "pdf-parse"
      via: "import { PDFParse }"
      pattern: "import \\{ PDFParse \\} from \"pdf-parse\""
    - from: "src/lib/render.test.ts"
      to: "fixtures/sample-resume.json"
      via: "readFile + JSON.parse"
      pattern: "sample-resume\\.json"
---

<objective>
Write `src/lib/render.test.ts` — a `node:test` suite that (a) unit-tests `resolveOutputPaths` for the D-O01/D-O02 naming contract and (b) renders the ATS PDF from `fixtures/sample-resume.json`, extracts its text via `pdf-parse` 2.x, and asserts every resume field appears in correct linear reading order. Then run a blocking human-verify checkpoint on the designed PDF (D-T02).

Purpose: Prove Success Criteria #2, #3, #4 from ROADMAP.md via automation, and prove #1 (designed PDF visual quality) via the manual inspection that D-T02 explicitly requires.
Output: Passing test suite + human-confirmed designed PDF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-designed-ats-pdf-rendering/03-CONTEXT.md
@.planning/phases/03-designed-ats-pdf-rendering/03-RESEARCH.md
@.planning/phases/03-designed-ats-pdf-rendering/03-01-SUMMARY.md
@src/lib/render.ts
@src/lib/extract.test.ts
@src/lib/preflight.test.ts
@fixtures/sample-resume.json
@tsconfig.test.json

<interfaces>
<!-- Contracts the executor needs — extracted from src/lib/render.ts (created in Plan 03-01) -->
<!-- and existing test conventions (src/lib/extract.test.ts, src/lib/preflight.test.ts). -->

Public API of src/lib/render.ts (from Plan 03-01):
```typescript
export async function renderDesigned(data: ResumeData, outputPath: string, browser: Browser): Promise<void>;
export async function renderAts(data: ResumeData, outputPath: string, browser: Browser): Promise<void>;
export function resolveOutputPaths(inputMdPath: string): { designed: string; ats: string };
```

Established test conventions (from src/lib/extract.test.ts, src/lib/preflight.test.ts):
- Test imports: `import assert from "node:assert/strict";` and `import { describe, it } from "node:test";`
- Lifecycle imports when needed: `import { before, after } from "node:test";`
- Test files run via: `npx tsx --test src/lib/render.test.ts`
- Path pattern: `const __dirname = dirname(fileURLToPath(import.meta.url)); const root = resolve(__dirname, "../..");`
- ESM `.js` extension on all relative imports even for `.ts` sources

pdf-parse 2.x API (from RESEARCH.md Pattern 4, verified via GitHub README):
```typescript
import { PDFParse } from "pdf-parse";
const parser = new PDFParse({ data: buffer });   // buffer: Buffer | Uint8Array
const result = await parser.getText();           // result: { text: string, ... }
await parser.destroy();                          // release resources
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write render.test.ts — resolveOutputPaths unit tests + ATS pdf-parse extraction test</name>
  <files>src/lib/render.test.ts</files>
  <read_first>
    - src/lib/render.ts (public API being tested; know the exact export names)
    - src/lib/extract.test.ts (path setup, describe/it structure, .js relative imports, no live API pattern)
    - src/lib/preflight.test.ts (lifecycle usage: `describe` + `it` + module-level assertions)
    - fixtures/sample-resume.json (exact content strings the extraction test must find in the PDF text)
    - .planning/phases/03-designed-ats-pdf-rendering/03-RESEARCH.md (Pattern 4 pdf-parse usage, Verified ATS Test Structure code example)
    - .planning/phases/03-designed-ats-pdf-rendering/03-CONTEXT.md (D-T01: node:test + pdf-parse — this is the corrected decision, not Vitest)
    - tsconfig.test.json (confirm src/lib/*.test.ts is in the include glob so tsc --noEmit -p tsconfig.test.json picks up the new file)
  </read_first>
  <behavior>
    resolveOutputPaths unit tests (3 cases):
    - Test 1: Given `/tmp/foo/my-resume.md`, returns `{ designed: "/tmp/foo/my-resume-resume.pdf", ats: "/tmp/foo/my-resume-resume-ats.pdf" }` — proves D-O01 (same directory) + D-O02 (stem + `-resume` / `-resume-ats` suffix).
    - Test 2: Given a relative path `./notes/alex.md`, returns paths whose dirname equals `./notes` (or path-normalized equivalent) and whose basenames are `alex-resume.pdf` and `alex-resume-ats.pdf`.
    - Test 3: Given a path with a stem containing a dot like `/x/2026.q3-resume.md`, the stem is `2026.q3-resume` (only the trailing `.md` is stripped — Node's `basename(p, ".md")` semantics), producing `2026.q3-resume-resume.pdf`.

    ATS extraction test suite (uses node:test `before` + `after` lifecycle for expensive one-time Puppeteer launch):
    - `before`: launch `puppeteer.launch({ headless: true })`, `mkdtemp(join(tmpdir(), "cvgen-test-"))`, load `fixtures/sample-resume.json`, call `renderAts(data, join(tmp, "test-resume-ats.pdf"), browser)`, then read the resulting PDF into a Buffer, run `new PDFParse({ data: buffer })`, `await parser.getText()`, `await parser.destroy()`, and store the extracted `text` on a module-scope variable.
    - `after`: `await browser.close()`.
    - Test A: `text` includes `"Alex Rivera"` (candidate name from fixture).
    - Test B: `text` includes `"alex@example.com"`, `"(555) 000-0000"`, `"San Francisco, CA"`, `"linkedin.com/in/alexrivera"`, `"github.com/alexrivera"` (contact block).
    - Test C: `text` includes the summary substring `"Senior software engineer with 10 years"`.
    - Test D: `text` includes every experience role/company pair — `"Senior Engineer"` with `"Acme Corp"`, and `"Platform Consultant"` with `"Tech Partners LLC"`.
    - Test E: `text` includes each experience bullet substring — at minimum `"Reduced API latency by 40%"` and `"Designed event-driven microservices"`.
    - Test F: `text` includes education — `"B.S. Computer Science"`, `"UCLA"`, `"2014"`.
    - Test G: `text` includes each skill category and at least one item per category — `"Languages"`, `"TypeScript"`, `"Tools"`, `"Docker"`.
    - Test H (linear reading order — the direct proof of Success Criterion #3): `text.indexOf("Alex Rivera") < text.indexOf("alex@example.com")` AND `text.indexOf("alex@example.com") < text.indexOf("Senior Engineer")` AND `text.indexOf("Senior Engineer") < text.indexOf("B.S. Computer Science")` AND `text.indexOf("B.S. Computer Science") < text.indexOf("Languages")`.
  </behavior>
  <action>
    Create `src/lib/render.test.ts` following the exact structure of `src/lib/extract.test.ts` (imports, __dirname derivation, describe/it blocks). Use `describe("resolveOutputPaths", ...)` for the three synchronous unit tests, and a second `describe("renderAts pdf-parse extraction", ...)` block that uses `before`/`after` from `node:test` to share a Puppeteer Browser and pre-render the ATS PDF exactly once — do NOT re-render inside every `it()` (Puppeteer launch is ~500ms; rendering per-test would balloon the test run).

    Read the fixture at `resolve(root, "fixtures/sample-resume.json")` using `readFile(..., "utf8")` + `JSON.parse` — do NOT use JSON import assertions (they are unstable across Node minor releases). Cast the parsed value with `as ResumeData` after `import type { ResumeData } from "../schema/resume.js";`.

    Do NOT delete the temp directory in `after` — leave the file so Task 2's human-verify checkpoint can also inspect the ATS PDF if needed; log the path via `console.log` in `before` for reference.

    Every content string asserted must come verbatim from `fixtures/sample-resume.json`; do not invent substrings. All the specific values are listed in the `<behavior>` block above.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.test.json</automated>
    <automated>grep -c "import { PDFParse } from \"pdf-parse\"" src/lib/render.test.ts</automated>
    <automated>grep -c "import { describe, it, before, after } from \"node:test\"" src/lib/render.test.ts</automated>
    <automated>! grep -E "from ['\"]vitest['\"]" src/lib/render.test.ts</automated>
    <automated>npx tsx --test src/lib/render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/render.test.ts` exists and imports `PDFParse` from `pdf-parse` (per D-T01 corrected decision — not Vitest)
    - `src/lib/render.test.ts` imports `describe`, `it`, `before`, `after` from `"node:test"`
    - `src/lib/render.test.ts` contains no `from "vitest"` substring
    - `src/lib/render.test.ts` contains at least 3 `it(` calls in the `resolveOutputPaths` describe block
    - `src/lib/render.test.ts` contains at least 8 `it(` calls in the ATS extraction describe block (Tests A–H)
    - `npx tsc --noEmit -p tsconfig.test.json` exits 0
    - `npx tsx --test src/lib/render.test.ts` exits 0 with all tests passing (report shows `# fail 0`)
    - The linear-order assertion (`text.indexOf("Alex Rivera") < text.indexOf("alex@example.com") < text.indexOf("Senior Engineer") < text.indexOf("B.S. Computer Science") < text.indexOf("Languages")`) evaluates true
  </acceptance_criteria>
  <done>Full `node:test` suite passes: 3 resolveOutputPaths unit tests + 8 ATS extraction tests all green, typecheck passes, no Vitest imports present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify designed PDF (D-T02)</name>
  <what-built>
    Plan 03-01 produced `scripts/smoke-render.ts` which renders both PDFs into a temp directory when run via `npm run smoke-render`. Task 1 of this plan produced `src/lib/render.test.ts` which additionally renders the ATS PDF into a separate temp directory (path logged to stdout during the test run). The designed PDF has NOT been visually inspected yet — D-T02 explicitly requires manual verification for the designed PDF.
  </what-built>
  <how-to-verify>
    1. Re-run `npm run smoke-render` and copy the printed `-resume.pdf` path (the designed PDF, not the `-ats` one).
    2. Open the designed PDF (`open <path-to-designed-pdf>` on macOS, or drag into a browser).
    3. Confirm the following, each of which corresponds to a locked decision or a ROADMAP success criterion:
       a. Font is Inter (geometric sans-serif — verify by comparing letterforms of the candidate name against a system Arial rendering; Inter has a distinctive lowercase `a` and `g`) — D-D02.
       b. A single subtle muted accent color is applied to the candidate name and/or section headers only (no rainbow, no bright hues) — D-D03.
       c. Layout is single-column top-to-bottom flow; there is no side-by-side content anywhere in the document — RENDER-01 / ROADMAP Success Criterion #1.
       d. No images or icons anywhere in the document — CLAUDE.md constraint.
       e. Generous whitespace and clean visual hierarchy consistent with "minimal / modern" (D-D01).
       f. No section header is stranded at the bottom of a page with all its content on the next page — RESEARCH.md Pitfall 3.
       g. Every fixture field is present and legible: candidate name, contact info, summary, core competencies, both experience entries with bullets, education, and both skill groups.
    4. Also open the `-ats.pdf` file side by side. Confirm it is CLEARLY visually distinct from the designed PDF at a glance — different font (Arial), no accent color, plainer layout (D-A03).
    5. If anything is wrong, describe the specific issue (e.g. "accent color is too bright — needs to be muted like #2d4a6b" or "Inter did not load — candidate name looks like Helvetica").
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 designed-PDF checks pass AND the ATS PDF is visually distinct; otherwise describe the specific issue and Claude will iterate on `src/lib/render.ts` and re-run `npm run smoke-render`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Rendered ATS PDF bytes → pdf-parse extraction | Test consumes PDF bytes produced by our own renderer; failure mode is functional (wrong text order, missing fields), not adversarial |
| Test process → Puppeteer / Chromium | Same-process trust boundary as Plan 03-01 Task 3; no new exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | ATS template producing text out of reading order (would break Success Criterion #3 without detection) | mitigate | Test H asserts `indexOf` ordering across the five section anchors (name → email → experience role → education degree → skills category); a regression that reorders sections in the ATS HTML fails this test loudly |
| T-03-05 | Information Disclosure | Hidden / near-invisible text in ATS PDF (out-of-scope per REQUIREMENTS.md, but no automated check for it exists) | accept | Human-verify checkpoint (Task 2 step 4) requires the reviewer to compare the ATS PDF side-by-side with the designed PDF and confirm no hidden text tricks are present. RESEARCH.md notes this is not automatable without OCR + font-color analysis; deferred to reviewer judgment for Phase 3. |
</threat_model>

<verification>
- `npx tsx --test src/lib/render.test.ts` exits 0 with all `describe` blocks green
- `npx tsc --noEmit -p tsconfig.test.json` exits 0
- Task 2 checkpoint returns "approved" from the human reviewer (or, if issues are raised, they are resolved and Task 1 tests still pass afterwards)
</verification>

<success_criteria>
- ATS PDF text extraction proves every fixture field appears in correct linear reading order (ROADMAP Success Criterion #3 — satisfies RENDER-02)
- `resolveOutputPaths` unit tests prove D-O01 (same directory) and D-O02 (stem-based `-resume.pdf` / `-resume-ats.pdf` naming) — satisfies RENDER-03
- Human reviewer confirms designed PDF meets D-D01 (minimal/modern), D-D02 (Inter loaded), D-D03 (muted accent color) and RENDER-01 (single-column, no images) — satisfies ROADMAP Success Criterion #1
- Human reviewer confirms designed and ATS PDFs are visually distinct at a glance — satisfies D-A03
- Test suite uses `node:test` + `pdf-parse` per corrected D-T01; no Vitest imports appear anywhere
</success_criteria>

<output>
Create `.planning/phases/03-designed-ats-pdf-rendering/03-02-SUMMARY.md` when done. Include the checkpoint outcome (approved / iterated) in the summary.
</output>
