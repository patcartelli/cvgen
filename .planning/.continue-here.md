---
context: default
phase: null
task: null
total_tasks: null
status: paused
last_updated: 2026-07-31T19:56:36.741Z
---

_No active phase. v1.1 complete. Session was quick task polish on PDF output._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| readline/promises EOF drop | `readline/promises` silently drops question() on stdin EOF | advisory | Use callback-based readline + line-buffer ask() helper (see src/cli/index.ts) |
| process.exit() in async Commander handler | Produces exit code 13, drops buffered stderr | advisory | Use writeSync + exitCode + stdin.destroy() + return |
| npm link from worktree | Global symlink points to worktree; dangling after cleanup | advisory | Re-run npm link from main checkout after worktree removal |

<current_state>
Between milestones. 6 quick tasks shipped this session, all committed. dist/ needs rebuild before testing.

**Today's changes (all in src/):**
- `render.ts` — 2-row contact header, clickable links, additionalExperience section, website in row 2
- `schema/resume.ts` — website (optional) + additionalExperience (optional) fields
- `cli/index.ts` — filename now {name_slug}-resume[-{Company}][-{date}].pdf; website in init template
</current_state>

<completed_work>
1. 260731-gby `92e1414` — 2-line contact header: email·phone·location / website·linkedin·github
2. 260731-gui `6971669` — Candidate name in filename: pat_cartelli-resume-Company-Date.pdf
3. 260731-h3z `66fcadd` — Date in filename; email/linkedin/github clickable in designed PDF
4. 260731-hck `8d42e68` — Optional website field in contact schema
5. `0cff081` — Row 2 order: website · linkedin · github
6. 260731-j41 `663018d` — additionalExperience field for ### Additional Experience sections
</completed_work>

<remaining_work>
- Run `npm run build` to rebuild dist/
- Fix Captivate_IQ.md in Obsidian (delete line 10 `---` and line 12 duplicate-frontmatter heading)
- Commit stale phase deletion entries in git status
- Define v1.2 milestone
</remaining_work>

<decisions_made>
- website is optional (not added to preflight required fields — existing resumes must not break)
- additionalExperience: italic-labeled compact list in designed PDF; h3+bullets in ATS
- Contact row 2 order: website · linkedin · github (user preference)
- PDF filename: {name_slug}-resume[-{Company}][-{YYYY-MM-DD}].pdf
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — quick tasks table has today's 6 entries
2. `.planning/PROJECT.md` — v1.2 candidates are in the Active requirements section

## Infrastructure State
- dist/ is STALE — `npm run build` required before testing
- `cvgen` linked globally via `npm link` → `/opt/homebrew/bin/cvgen`
- 21 automated tests passing (render.test.ts only — tsx --test)
- `tsc --noEmit` clean

<context>
Productive afternoon of quick tasks. No phase work. The PDF output is now much more polished — contact header is 2 rows, filenames are properly identified and dated, links work in PDF readers, website and additional experience are supported. Ready for v1.2 scoping.
</context>

<next_action>
1. `npm run build` — rebuild dist/ with today's changes
2. Fix Captivate_IQ.md in Obsidian (delete stray --- on line 10 and heading on line 12)
3. `cvgen "path/to/Captivate_IQ.md"` — test against your real resume
4. `/gsd-new-milestone` — define v1.2 when ready
</next_action>
