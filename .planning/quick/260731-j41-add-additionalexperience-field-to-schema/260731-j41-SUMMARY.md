---
quick_id: 260731-j41
slug: add-additionalexperience-field-to-schema
status: complete
date: 2026-07-31
commit: 663018d
---

# Quick Task 260731-j41: additionalExperience field + CV file review

## CV file issues found (fix in Obsidian)

- Line 10: stray `---` separator after frontmatter — remove it
- Line 12: heading that repeats all frontmatter fields as text — delete the entire line

## What was done

Added `additionalExperience?: string[]` to `ResumeSchema`. Claude will populate it from `### Additional Experience` bullets. Renders as:
- **Designed PDF:** compact italic-labeled list at the bottom of the Experience section
- **ATS PDF:** `<h3>Additional Experience</h3>` + bullets

Optional — existing resumes without the section are unaffected.

## Files changed

- `src/schema/resume.ts` — `additionalExperience: z.array(z.string()).optional()`
- `src/lib/render.ts` — CSS + HTML for both designed and ATS templates

21/21 tests pass.
