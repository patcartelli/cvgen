---
phase: quick-260729-ez3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - package.json
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - portfolio-ready public repo
must_haves:
  truths:
    - Cold visitor to GitHub can understand what cvgen does and how to install it from README alone
    - Running `npm link` after `npm install` produces a working global `cvgen` binary that calls current Phase 4 CLI code
    - Pushing to main triggers CI that installs deps and runs 38-test suite without requiring ANTHROPIC_API_KEY
  artifacts:
    - path: README.md
      provides: Public-facing project documentation
      contains: install, usage, prerequisites, flags
    - path: package.json
      provides: files field limiting npm pack output to dist/
      contains: '"files": ["dist"]'
    - path: .github/workflows/ci.yml
      provides: GitHub Actions CI on push/PR to main
      contains: node-version, npm ci, npm test
  key_links:
    - from: package.json bin field
      to: dist/cli/index.js
      via: tsc build
      pattern: dist/cli/index.js must exist and be current Phase 4 output
---

<objective>
Polish the public cvgen repo so it is portfolio-ready: write a useful README, rebuild dist/ from current source so the bin entry works, and add GitHub Actions CI.

Purpose: v1.0 is tagged and complete; the repo is public but not usable by a cold visitor — README is a stub, the dist is stale (Phase 2 argv stub, not Phase 4 Commander CLI), and there is no CI badge or automated test runner.
Output: README.md, updated package.json (files field), rebuilt dist/, .github/workflows/ci.yml
</objective>

<execution_context>
@/Users/pcartelli/dev/cvgen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pcartelli/dev/cvgen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/pcartelli/dev/cvgen/.planning/STATE.md
@/Users/pcartelli/dev/cvgen/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Replace the 47-byte stub with a proper project README. Structure:

1. Header: `# cvgen` with a one-sentence tagline (CLI that converts an Obsidian markdown resume note into a portfolio-quality PDF and an ATS-safe PDF via Claude API + Puppeteer).

2. Prerequisites section: Node 22+, an ANTHROPIC_API_KEY environment variable (link to Anthropic console or note it must be set in shell/dotenv). No other secrets needed.

3. Install section — two steps:
   a. `git clone` + `cd cvgen` + `npm install`
   b. `npm link` to install the global `cvgen` binary

4. Usage section — document the real Commander CLI surface:
   - `cvgen <path/to/resume.md>` — main command, outputs two PDFs alongside the input file
   - `--verbose` flag — prints Claude API response and parse details
   - `--validate-only` flag — runs extraction and prints JSON without rendering PDFs
   - `cvgen init` subcommand — scaffolds a starter resume markdown file

5. Output files section: briefly name the two output files and distinguish portfolio PDF vs ATS-safe PDF.

6. Development section (brief): `npm run dev`, `npm test`, `npm run build`.

7. License: MIT (or note if package.json has no license field — if missing, state "see LICENSE" and leave as-is; do not invent a license file).

Do not add project-structure tree or lengthy architecture explanation — keep the README scannable in under 60 seconds.
  </action>
  <verify>
    <automated>wc -c README.md | awk '{if ($1 > 500) print "PASS: README is " $1 " bytes"; else print "FAIL: README too short at " $1 " bytes"}'</automated>
  </verify>
  <done>README.md exceeds 500 bytes, covers prerequisites, install, and all four CLI surface points (main command, --verbose, --validate-only, cvgen init).</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild dist and add files field to package.json</name>
  <files>package.json, dist/ (generated)</files>
  <action>
Two steps:

Step A — Add `"files": ["dist"]` to package.json. Insert it after the `"engines"` block (before `"bin"`). This ensures `npm pack` and `npm publish` only ship the compiled output, not src/ or test files.

Step B — Run `npm run build` (which invokes `tsc`) to compile current source into dist/. The existing tsconfig.json targets dist/ as outDir. After the build, confirm `dist/cli/index.js` exists and contains a shebang (`#!/usr/bin/env node`) and Commander CLI wiring — NOT the Phase 2 argv stub.

Do not modify tsconfig.json or any source files. The build command is already defined in package.json scripts.
  </action>
  <verify>
    <automated>node /Users/pcartelli/dev/cvgen/dist/cli/index.js --help 2>&1 | grep -q "cvgen" && echo "PASS: dist/cli/index.js responds to --help" || echo "FAIL: binary unresponsive"</automated>
  </verify>
  <done>package.json contains `"files": ["dist"]`; `dist/cli/index.js` exists and `node dist/cli/index.js --help` prints Commander help output (not a raw error or argv dump).</done>
</task>

<task type="auto">
  <name>Task 3: Add GitHub Actions CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Create `.github/workflows/ci.yml`. The workflow must:

- Name: `CI`
- Trigger: `push` and `pull_request` targeting `main`
- Single job named `test`, runs on `ubuntu-latest`
- Steps:
  1. `actions/checkout@v4`
  2. `actions/setup-node@v4` with `node-version: '22'` and `cache: 'npm'`
  3. `npm ci`
  4. `npm test`

No secrets are referenced — the 38-test suite is local and does not call the Claude API.

Do not add a typecheck step, lint step, or build step. Keep it minimal: install + test only. The test command is `tsx --test src/**/*.test.ts` (already in package.json scripts as `npm test`).

Create the `.github/workflows/` directory tree as needed — it does not exist yet.
  </action>
  <verify>
    <automated>cat /Users/pcartelli/dev/cvgen/.github/workflows/ci.yml | grep -c "npm test" | xargs -I{} sh -c 'if [ {} -ge 1 ]; then echo "PASS: ci.yml contains npm test"; else echo "FAIL: npm test not found"; fi'</automated>
  </verify>
  <done>.github/workflows/ci.yml exists, references node-version 22, runs `npm ci` then `npm test`, triggers on push and PR to main, contains no secret references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → npm registry | npm ci installs packages from registry on each CI run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-ez3-01 | Tampering | .github/workflows/ci.yml | accept | Workflow only runs read-only test suite; no secrets, no deploy steps, no write permissions requested — attack surface is negligible |
| T-ez3-02 | Spoofing | README ANTHROPIC_API_KEY instructions | accept | README instructs user to set env var from their own Anthropic account; no credential is committed or suggested to be committed |
</threat_model>

<verification>
All three tasks can be verified locally before pushing:

1. README: `wc -c README.md` returns > 500 bytes; scan content manually for the four CLI surface points.
2. dist: `node dist/cli/index.js --help` returns Commander usage output with `cvgen` name; `grep '"files"' package.json` confirms field present.
3. CI: `cat .github/workflows/ci.yml` shows correct trigger, node-version 22, `npm ci`, `npm test`.
</verification>

<success_criteria>
- README is useful to a cold GitHub visitor: describes the tool, prerequisites, install steps, and all CLI flags/subcommands.
- `npm link` produces a working global `cvgen` binary backed by current Phase 4 Commander CLI source.
- .github/workflows/ci.yml exists and would pass YAML validation; the test step matches the existing `npm test` script.
- package.json has `"files": ["dist"]` so `npm pack` output is clean.
</success_criteria>

<output>
Create `/Users/pcartelli/dev/cvgen/.planning/quick/260729-ez3-three-portfolio-polish-tasks-readme-md-r/260729-ez3-SUMMARY.md` when done.
</output>
