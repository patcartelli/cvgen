---
phase: quick-260819-i5h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/schema/resume.ts
  - src/lib/render.ts
  - src/lib/extract.ts
  - src/schema/validate.test.ts
  - src/lib/render.test.ts
  - src/lib/extract.test.ts
autonomous: true
must_haves:
  truths:
    - "Nested client work extracts as experience[].engagements[], not sibling jobs"
    - "Header selected-work URL+password renders in both PDF templates"
    - "Designed PDF uses a comma between company and role; date ranges use an en dash; no em dashes"
    - "ExperienceSchema.type is unchanged (Patrick deferred the badge decision)"
    - "Designed PDF is at most 2 pages; if over, tighten template before trimming master copy"
  artifacts:
    - path: src/schema/resume.ts
      provides: "EngagementSchema + optional engagements[] + top-level selectedWork"
    - path: src/lib/render.ts
      provides: "Indented engagements, header selected work, en dashes, no em dashes"
---

<objective>
Stop the designed PDF from flattening nested Studio Cartelli clients into parallel 2026 jobs, and stop dropping the header portfolio link. Leave the type badge as-is.
</objective>

<tasks>

<task type="auto">
  <name>Schema: engagements[] and selectedWork</name>
  <files>src/schema/resume.ts, src/schema/validate.test.ts, src/lib/extract.ts, src/lib/extract.test.ts</files>
  <action>
    Add EngagementSchema { client, role, startDate, endDate?, bullets[] } with Zod .describe() so extract.ts stays a thin wrapper. Add optional experience.engagements and top-level selectedWork { url, password? }. Reuse the case-study object shape. Describe bullets as "only source text, never invent." Bump extract max_tokens to 4096. Add one system-prompt sentence: extract only source content. Do not change type enum. Tests: schema accepts nested engagements + selectedWork; existing fixtures still validate; extract.ts assertions updated for max_tokens.
  </action>
  <verify>npx tsx --test src/schema/validate.test.ts src/lib/extract.test.ts</verify>
  <done>Nested client work has a schema slot; selected work has a top-level slot; extract still uses messages.parse + ResumeSchema.</done>
</task>

<task type="auto">
  <name>Templates: nest, header link, punctuation, page cap</name>
  <files>src/lib/render.ts, src/lib/render.test.ts</files>
  <action>
    Designed: indent engagements under parent; selectedWork in the contact header; company and role joined with a comma; dates keep en dash. ATS: linear "Client engagement:" lines; dates use en dash not &amp;mdash;. Lift URL+password display from existing case-study lines. Type badge rendering unchanged. After a live extract+render of the master, if designed PDF exceeds 2 pages, tighten spacing/margins before any copy trim. Agreed copy trims if still over: Signafire, then Wellhub. Never trim Bluefish or Studio Cartelli. Never invent copy in JSON or HTML.
  </action>
  <verify>npx tsx --test src/lib/render.test.ts && pdf-parse page count on regenerated designed master</verify>
  <done>Both templates render nested engagements and header selected work; no em dashes; designed PDF ≤ 2 pages or an explicit remaining-trim note.</done>
</task>

</tasks>
